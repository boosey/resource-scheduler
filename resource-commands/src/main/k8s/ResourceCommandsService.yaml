apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: resource-commands-server
  namespace: resource-scheduler
spec:
  template:
    metadata:
      name: resource-commands-server-v39
      annotations:
        autoscaling.knative.dev/target: "1"
    spec:
      containers:
        - image: docker.io/boosey/resource-command-server