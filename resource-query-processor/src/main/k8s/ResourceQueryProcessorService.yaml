apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: resource-query-processor
  namespace: resource-scheduler
spec:
  template:
    metadata:
      name: resource-query-processor-v18
      annotations:
        autoscaling.knative.dev/target: "1"
    spec:
      containers:
        - image: docker.io/boosey/resource-query-processor